import { MouseDragScrollDirective } from './mouse-drag-scroll.directive';

describe('MouseDragScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseDragScrollDirective();
    expect(directive).toBeTruthy();
  });
});
