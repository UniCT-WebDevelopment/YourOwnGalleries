import { DownloadsDirective } from './downloads.directive';

describe('DownloadsDirective', () => {
  it('should create an instance', () => {
    const directive = new DownloadsDirective();
    expect(directive).toBeTruthy();
  });
});
